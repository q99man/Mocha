# Mocha Handoff 2026-04-23

## Current Workspace State

This handoff captures the current in-progress workspace so work can continue from another machine/IDE without losing context.

- Many files are already staged. Do not assume the branch is clean.
- Some files still have unstaged edits from parallel IDE work or follow-up UI tweaks.
- Untracked items still exist and should be reviewed before commit: `frontend/scratch/`, `frontend/src/scratch_line_72.json`.
- `backend/src/main/resources/application-mysql.yml` is modified but not staged. Treat it as local/environment-sensitive until reviewed.
- Important: do not revert unrelated UI edits. The working agreement is to merge UI changes from the other IDE and keep functional work here.

## Completed In This Session

### CSS Split / Landing Recovery

- Continued recovery after CSS split regressions.
- Fixed landing page review card sizing and mobile review carousel behavior.
- Removed the review carousel scrollbar once animation worked again.
- Footer menu links were removed and replaced with simple static website footer copy:
  - Brand sentence
  - Copyright
  - Address / office location
  - Contact hours / email
  - Basic policy/service notice copy

Main files:

- `frontend/src/features/landing/LandingFooter.tsx`
- `frontend/src/pages/HomePage.tsx`
- `frontend/src/app/landing.css`

### Toast / Confirm UX

- Custom confirm/toast flow was extended across more actions.
- Login/write flows were already being moved toward custom toast behavior.
- Board comment create/update/delete now uses `CompactToast` instead of inline message text.
- Challenge review create/update/delete now uses `CompactToast` instead of inline message text.
- MyPage post/review create/update/delete feedback now uses `CompactToast`.
- Logout confirm/toast work was part of the earlier UI pass.

Main files:

- `frontend/src/pages/BoardDetailPage.tsx`
- `frontend/src/pages/ChallengesPage.tsx`
- `frontend/src/features/challenges/ChallengeReviewsPane.tsx`
- `frontend/src/pages/MyPage.tsx`
- `frontend/src/shared/components/CompactToast.tsx`

### Admin Hub / Model / Challenge Management

- Admin hub UI was moved toward a compact unified pattern.
- Action buttons use the shared admin icon/action-button style.
- `frontend/src/shared/components/AdminIcons.tsx` was added and is now used from admin and MyPage UI.
- Challenge management actions were reorganized:
  - Row action area was adjusted multiple times for responsive behavior.
  - Active/inactive action was moved back into expanded challenge detail.
  - Detail area keeps analysis-related actions together.
- Model management supports selecting/activating one of the uploaded pose landmarker models.
- Pose landmarker versions were discussed:
  - `lite`: fastest/smallest, lower accuracy.
  - `full`: balanced default.
  - `heavy`: most accurate/heaviest, slower and more resource intensive.

Main files:

- `frontend/src/pages/AdminHubPage.tsx`
- `frontend/src/features/admin/AdminChallengesSection.tsx`
- `frontend/src/features/admin/AdminAssetsSection.tsx`
- `frontend/src/pages/AdminModelAssetsPage.tsx`
- `frontend/src/shared/components/AdminIcons.tsx`
- `frontend/src/app/admin.css`

### Pagination Standardization

- List page size was standardized to 10 items where applicable.
- Challenge page list now has pagination and shows 10 challenges per page.
- Board main list changed from 20 to 10.
- Admin hub lists changed to 10:
  - Challenges
  - Model assets
  - Members
  - Board posts
- MyPage was already 10 in most UI constants; board API defaults were updated from 4 to 10.
- Backend admin member default page size changed from 8 to 10.

Main files:

- `frontend/src/pages/ChallengesPage.tsx`
- `frontend/src/pages/BoardPage.tsx`
- `frontend/src/pages/AdminHubPage.tsx`
- `frontend/src/pages/MyPage.tsx`
- `frontend/src/shared/api/boardApi.ts`
- `backend/src/main/java/com/motionchallenge/admin/controller/AdminMemberController.java`

### Board Admin Count / Pagination Detail

- Investigated admin board summary count and impossible page numbers.
- Backend uses physical delete for board posts, not soft delete.
- Overview/list count behavior was tightened and covered with an integration test.
- Board overview now exposes category counts used by admin UI:
  - `totalCount`
  - `generalCount`
  - `noticeCount`
  - `freeCount`
  - `reviewCount`
- Frontend board delete flow now reloads the correct page when deleting the last item on a page.
- Shared `Pagination` clamps an invalid current page to a safe page.

Main files:

- `backend/src/main/java/com/motionchallenge/board/dto/BoardOverviewResponse.java`
- `backend/src/main/java/com/motionchallenge/board/repository/BoardPostRepository.java`
- `backend/src/main/java/com/motionchallenge/board/service/BoardService.java`
- `backend/src/test/java/com/motionchallenge/board/controller/BoardControllerIntegrationTest.java`
- `frontend/src/pages/AdminHubPage.tsx`
- `frontend/src/shared/components/Pagination.tsx`
- `frontend/src/shared/types/board.ts`

### MyPage UI / UX

- MyPage was aligned closer to the current Admin Hub compact UI.
- Attempts/posts/reviews tabs now reuse admin compact table/action styling.
- Post/review actions use icon buttons and shared action grouping.
- Inline success/error messages were removed from post/review sections and replaced by page-level `CompactToast`.

Main files:

- `frontend/src/pages/MyPage.tsx`
- `frontend/src/features/mypage/MyPageAttemptsTab.tsx`
- `frontend/src/features/mypage/MyPagePostsTab.tsx`
- `frontend/src/features/mypage/MyPageReviewsTab.tsx`

### Auth / OAuth Support

- OAuth disabled fallback controller was added so local disabled OAuth routes do not return raw backend 404.
- Auth integration test was updated earlier.

Main files:

- `backend/src/main/java/com/motionchallenge/member/oauth/OAuth2FallbackController.java`
- `backend/src/test/java/com/motionchallenge/member/controller/AuthIntegrationTest.java`
- `frontend/src/shared/auth/AuthModal.tsx`
- `frontend/src/shared/api/client.ts`

## Verification Run

Frontend:

- `cd frontend; npm.cmd run build` passed after the latest footer, challenge review toast, MyPage, and pagination changes.

Backend:

- Targeted board integration test passed earlier:
  - `./gradlew.bat test --tests com.motionchallenge.board.controller.BoardControllerIntegrationTest`
- Auth integration test passed earlier:
  - `./gradlew.bat test --tests com.motionchallenge.member.controller.AuthIntegrationTest`
- Full backend build was attempted:
  - `./gradlew.bat build`
  - Compile passed.
  - Test phase failed in `MotionCalibrationSampleReportTest.motionCalibrationSamplesProduceUsefulScoreOrderingAndJudgementDistribution()`.
  - This appears unrelated to the UI/pagination/toast work and is in motion calibration sample scoring.

Known non-blocking warnings:

- Several files show CRLF-to-LF warnings when staged/touched on Windows.

## Current Staged Scope

The staged set currently includes many files from multiple related tasks, including:

- Board overview/count backend and tests.
- OAuth fallback controller and auth test.
- Admin hub compact UI and pagination changes.
- Landing CSS/footer changes.
- Challenge page pagination and review toast.
- MyPage compact UI and toast changes.
- Shared admin icons and pagination.
- Split CSS imports and layout components.

Before committing, review staged diff as one combined feature/UI stabilization batch, or split into smaller commits manually.

Suggested commands:

```powershell
git status --short
git diff --cached --stat
git diff --cached -- frontend/src/pages/ChallengesPage.tsx frontend/src/features/challenges/ChallengeReviewsPane.tsx
git diff --cached -- frontend/src/pages/MyPage.tsx frontend/src/features/mypage
git diff --cached -- frontend/src/features/landing/LandingFooter.tsx frontend/src/app/landing.css
```

## Known Remaining Work / Watch Items

- Review untracked scratch files and decide whether to delete or ignore:
  - `frontend/scratch/`
  - `frontend/src/scratch_line_72.json`
- Review unstaged local config change:
  - `backend/src/main/resources/application-mysql.yml`
- Review unstaged UI files before final commit:
  - `frontend/src/features/challenges/ChallengeReferencePosePreview.tsx`
  - `frontend/src/shared/components/CompactFilterDropdown.tsx`
  - Any `MM` files in `git status --short`
- Admin Hub mobile responsiveness was the major theme. If anything still looks off, prefer consolidating shared compact table/action CSS rather than one-off fixes.
- Challenge management pagination alignment issue was investigated heavily. The current approach keeps shared pagination behavior, but visual verification on mobile is still recommended.
- Because UI edits are also happening in another IDE, always inspect `git status --short` before editing and merge instead of reverting.

## Recommended Next Start

1. Pull/open this workspace and run:

```powershell
git status --short
git diff --cached --stat
```

2. Start the dev server and manually smoke test:

```powershell
cd frontend
npm.cmd run dev
```

3. Check these flows in the browser:

- Landing page footer looks natural on desktop/mobile.
- Landing review carousel still animates on mobile and has no scrollbar.
- Challenge list paginates 10 at a time.
- Challenge review create/update/delete shows custom toast.
- Board detail comment create/update/delete shows custom toast.
- MyPage posts/reviews show compact Admin Hub-style actions and toast feedback.
- Admin Hub board summary counts and board pagination after delete still behave correctly.

4. If ready, decide commit split:

- Commit 1: backend board count/page-size/OAuth fallback.
- Commit 2: admin hub compact UI/model/challenge management.
- Commit 3: pagination standardization.
- Commit 4: toast UX for comments/reviews/MyPage.
- Commit 5: landing footer/review carousel polish.

5. Before final push/release, rerun:

```powershell
cd frontend
npm.cmd run build
```

For backend, prefer targeted tests first because the full build currently fails in unrelated motion calibration sample scoring:

```powershell
cd backend
$env:GRADLE_USER_HOME='C:\Users\tj-bu-702-01\Documents\GitHub\gneul\backend\.gradle-user-home'
./gradlew.bat test --tests com.motionchallenge.board.controller.BoardControllerIntegrationTest
./gradlew.bat test --tests com.motionchallenge.member.controller.AuthIntegrationTest
```
