# 이벤트 / 상태 계약

이 문서는 모션 세션 화면과 백엔드 상태 계약을 맞추기 위해 작성된 보관 문서입니다.

## 문서 목적

- 업로드, 분석, 점수 계산, 재시도 흐름에서 상태 이름을 일치시킨다.
- 시작 화면과 백엔드 응답이 서로 다른 의미를 가지지 않도록 한다.
- runtime state와 readiness state를 UI에서 일관되게 노출한다.

## 현재 기준

- 현재 사용 중인 계약은 실제 DTO와 프런트 상태 매핑 코드가 기준입니다.
